{
  "taskId": "AVIV-118",
  "request": "Remove the entire \"Done\" board story from OpenForge and replace it with a single terminal action. (1) Remove every way to move a task to Done: the TaskContextMenu \"Move to Done\" item + handler; the TaskDetailView status-selector Done option and the newStatus==='done' branch in handleStatusChange; the actionPalette 'move-to-done' action, its controller case, and taskActionRunner.moveTaskToDone; delete src/lib/moveToComplete.ts and its test. (2) Remove the cleanup-on-done backend path in src-tauri/src/app_invoke/core.rs update_task_status (both `if status == Done` blocks — workspace-completed and cleanup_task_runtime_for_app); keep cleanup_task_runtime_for_app because delete_task uses it. Remove clear_done_tasks (Rust handler + clearDoneTasks IPC wrapper + electronMigrationContracts entry + test). (3) Remove the Reopen story: src/lib/reopenTask.ts (+test), FocusBoard handleReopen + onReopen wiring, TaskContextMenu Reopen item/handler/onReopen prop. (4) Remove the Done lane/filter UI: FocusBoard FILTER_OPTIONS done entry + the cmd-4 shortcut + the done grouping branches; boardFilters 'done' filter value/branch/counter; FocusEmptyState 'done' entry — but KEEP the task.status==='done' EXCLUSION guards so legacy done rows remain hidden. (5) Rename Delete to Complete: the TaskContextMenu item becomes a flag + \"Complete\" (e.g. \"Complete 🏁\") that, after a confirmation prompt (\"Complete this task? Its worktree and branch will be deleted — this cannot be undone.\"), calls the existing deleteTask (deletes task + worktree + branch). Rename the action-palette delete action label to \"Complete\". Give the task-detail view a Complete (delete) affordance so it still has a terminal action. (6) Leave existing 'done' rows hidden with NO migration: keep 'done' as a recognized-but-unreachable status — Rust BoardStatus::Done variant + normalize/as_str, the TS 'done' type union and taskState/taskStatePresentation handling, and the migrations.rs allow-list that avoids rewriting 'done' rows — but nothing may produce or surface 'done' anymore. Constraints: do not disable eslint/TS errors (fix them); no hardcoded hex colors; tests assert business logic only (no CSS/visual assertions); Svelte 5 runes; import type per verbatimModuleSyntax. This was approved by the user, including the confirmation prompt on Complete.",
  "maxOracleIterations": 1,
  "targetOracleScore": 90
}
