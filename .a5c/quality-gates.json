{
  "qualityThreshold": 80,
  "testCoverage": {
    "minimum": null,
    "notes": "No coverage threshold is currently enforced by the project. Maintain existing behavior and add focused business-logic tests for touched areas."
  },
  "linting": {
    "tools": [
      {
        "name": "TypeScript compiler",
        "command": "pnpm exec tsc --noEmit",
        "appliesTo": ["src", "packages", "plugins"]
      },
      {
        "name": "Cargo compiler/tests",
        "command": "cargo test",
        "workingDirectory": "src-tauri",
        "appliesTo": ["src-tauri"]
      }
    ]
  },
  "formatting": {
    "tools": [],
    "notes": "No project-wide formatter command is currently configured. Follow existing Svelte/TypeScript/Rust style and AGENTS.md conventions."
  },
  "commitChecks": [
    "Run pnpm exec tsc --noEmit for frontend/type changes",
    "Run pnpm test for frontend/business-logic changes",
    "Run cargo test from src-tauri for Rust/backend changes",
    "Run pnpm build:plugins plus targeted host/runtime tests for plugin platform changes",
    "Run a Babysitter-tracked review step for product-code or behavior implementation changes before final handoff"
  ],
  "deployGates": [
    "Frontend typecheck and tests pass",
    "Rust tests pass",
    "Relevant plugin builds/tests pass when plugin platform code changed",
    "No unreviewed changes to CI/release packaging",
    "Review findings from the Babysitter-tracked review step are resolved or explicitly documented"
  ],
  "cicdIntegration": {
    "enabled": false,
    "notes": "Babysitter CI/CD integration was intentionally skipped during project install."
  }
}
