KVG-5075 implements the project-only first-page slice. Task-workspace support and page/zoom controls remain in KVG-5077 and KVG-5076. See [verification.md](verification.md) for completed project work, passing checks, and open completion gates; unchecked broader tasks may be partially implemented.

## 1. Secure document-read module

- [ ] 1.1 Add failing Rust tests for the new document-read interface using real temporary project/task roots. Cover exact 16 MiB, over-limit, uppercase PDF extensions, header rejection, empty input, and unchanged metadata-only PDF reads. Verify the tests fail because the capability is missing, not because fixtures are invalid.
- [ ] 1.2 Implement descriptor-relative root containment and regular-file opening for document reads, rejecting traversal, absolute/drive/UNC/URL paths and all descendant symlinks/reparse points. Add component-swap and nonregular-file tests first. Verify no bytes escape the selected root and no special-file open hangs on supported desktop platforms; fail closed where secure opening is unavailable.
- [ ] 1.3 Implement bounded read-plus-one-byte detection, ready/unavailable results, revision digest, and observed-file-change detection. Verify task 1.1 is green and growing/shrinking/replaced-file fixtures produce the specified result without unbounded reads.
- [x] 1.4 Add shared two-request admission, no wait queue, a 15-second response deadline, sanitized error prefixes, and cooperative cancellation. Verify timed-out/cancelled blocking I/O retains its admission slot until it exits, late results are discarded, and all exits then release resources without exposing absolute host paths.

## 2. Host, IPC, and public contract

- [ ] 2.1 Add failing app-invoke and plugin-host contract tests for project/task document reads, missing roots, workspace changes during reading, trusted admission, and no project fallback. Wire both adapters to the same secure reader and recheck workspace identity before publication. Verify all adapter tests pass with equivalent policy results.
- [x] 2.2 Add typed desktop commands and wrappers exported by `src/lib/ipc.ts`, command domain registration, and generated IPC registry entries. Verify camelCase payload mappings in IPC tests, denial of untrusted renderer requests, and `pnpm electron:contract:check`.
- [ ] 2.3 Export `DocumentPreviewRead`, `fs.readDocument`, and `fs.task.readDocument` through the public SDK. Update frontend host/runtime adapters, unavailable-runtime stubs, backend plugin runtime mappings, SDK fixtures, and test API builders. Verify frontend/backend SDK contract tests cover ready, unavailable, rejection, and older-host outcomes without changing `FileContent` document semantics.
- [ ] 2.4 Add full-size wire round-trip tests for the 22,369,624-character maximum base64 payload through app-invoke and backend plugin transport. Verify byte equality, raw size, revision, and no truncation; do not increase unrelated transport limits to bypass a failure.
- [ ] 2.5 Document the additive capability, result union, exact error prefixes, limits, trust model, stricter document symlink policy, byte ownership, and unavailable-host migration behavior in SDK authoring documentation. Verify public declaration/contract checks and retain regression assertions for every existing preview kind.

## 3. Renderer package and trusted assets

- [x] 3.1 Pin a supported `pdfjs-dist` release in File Viewer and package its matching self-contained worker, CMaps, fonts, decoding resources, and text-layer styles. Verify the selected public APIs against that release, record license notices, register emitted `frontendStyles`, and confirm the built plugin contains every referenced asset.
- [x] 3.2 Add failing production-policy tests, then permit only the specified trusted-renderer Blob worker and plugin resource fetches. Verify explicit `worker-src`, package-root asset containment, continued file-URL and untrusted-origin denial, and unchanged unsafe-evaluation restrictions in Electron trust/protocol tests.
- [x] 3.3 Build the PDF renderer module around local byte input and an explicitly owned real worker, with scripting, XFA, forms, links, attachments, and document actions inactive. Verify a real-worker browser test renders local content offline, blocks document-triggered actions and requests, and reports worker failure without a fake-worker fallback.
- [ ] 3.4 Add resource-budget tests first, then enforce 2,000 pages, one active page render, image/canvas limits, reduced-resolution reporting, and parse/render deadlines. Verify pathological dimension, compressed-image, many-page, and stalled-render fixtures stay within configured allocations or fail visibly and terminate their worker.

## 4. File Viewer ownership and controls

- [ ] 4.1 Extend the workspace source with document reads and add a PDF preview controller using workspace/path/reload identity. Verify project/task routing, visible-selection-only reads, older-host fallback, and no byte requests for directory listings or inactive previews through focused workspace/controller tests.
- [x] 4.2 Add failing lifecycle tests, then implement generation-based stale-result rejection and explicit identity transitions. Verify selection/workspace changes, retry, pane hide, plugin unload, failed startup, and unmount cancel work, terminate workers, revoke worker URLs, and clear buffers/canvas/text resources idempotently. Do not put prop-keyed disposal in effect cleanup.
- [ ] 4.3 Add a dedicated PDF preview view and keep `FileContentViewer` limited to dispatching PDF versus other preview kinds. Implement page input, Previous/Next, percentage zoom, Fit width, and wrapping toolbar controls. Verify page/zoom bounds, invalid entries, fit-width resize, no rereads on page/zoom changes, and unchanged behavior of other preview kinds.
- [ ] 4.4 Add synchronized text and structure layers, loading phases, metadata retention, specific unavailable/error messages, manual Retry, and return-to-tree behavior. Verify tagged/untagged/scanned, password-required, corrupt, empty, too-large, timeout, and page-error states through observable DOM and accessible-name assertions.
- [ ] 4.5 Add representative File Viewer PDF stories and browser interaction checks for project and task contexts. Verify visible focus, keyboard traversal, text selection after zoom, status announcements, and toolbar usability at 320 CSS pixels and 200% application zoom; review the layout before running the broad validation matrix.

## 5. Cross-system validation and handoff

- [x] 5.1 Run the full affected Rust crate suite and static/build checks from the crate root resolved by `scripts/rust-sidecar-layout.mjs`: `cargo test`, `cargo check`, `cargo build`, and `cargo clippy`. Verify secure-open fixtures on supported desktop OSes and record any platform coverage gap.
- [ ] 5.2 Run File Viewer `test` and `build`; Plugin SDK `test`, `build`, `check:entrypoints`, and `check:contract`; root `pnpm test`, `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm electron:contract:check`, and `pnpm packages:metadata:check`. Verify affected runtime code is included by its owning test runner and report any missing package-specific static checks.
- [ ] 5.3 Build and install the plugin, then verify real PDF.js rendering under packaged Electron production CSP with offline multilingual/tagged fixtures and malicious-action fixtures. Record peak memory and cleanup across repeated open/change/hide/close cycles, and perform keyboard plus screen-reader checks of tagged and scanned PDFs. Report failures and skipped checks rather than treating mocked rendering as sufficient.
- [ ] 5.4 Confirm the implementation satisfies both delta specs, keeps unrelated cleanup in KVG-5072 and KVG-5074, and updates task-scoped Handoff Notes through the plugin commands. Verify the final notes replacement succeeds and the completion report distinguishes shipped behavior from remaining validation gaps.
