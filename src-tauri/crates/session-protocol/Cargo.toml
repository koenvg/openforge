[package]
name = "openforge-session-protocol"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"

[dependencies]
openforge-session-host = { path = "../session-host" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
thiserror = "2"
