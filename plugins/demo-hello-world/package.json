{
  "name": "@openforge/plugin-demo-hello-world",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @openforge/plugin-sdk build && pnpm run build:bundle",
    "build:bundle": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@openforge/plugin-sdk": "workspace:*"
  },
  "peerDependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.3.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.0"
  },
  "openforge": {
    "id": "com.openforge.demo-hello",
    "apiVersion": 1,
    "displayName": "Hello World Demo",
    "description": "A minimal demo plugin that validates the OpenForge plugin system",
    "icon": "plug",
    "frontend": "./dist/frontend.js",
    "requires": [
      "views",
      "taskPane",
      "settings",
      "commands"
    ]
  }
}
