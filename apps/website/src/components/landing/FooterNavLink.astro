---
import type { FooterLink } from '../../lib/landingContent';

interface Props {
  link: FooterLink;
}

const { link } = Astro.props;
---

<li>
  <span class="footer-link-label">{link.label}</span>
  <a href={link.href}>
    {link.text}
    <span aria-hidden="true">→</span>
  </a>
</li>
